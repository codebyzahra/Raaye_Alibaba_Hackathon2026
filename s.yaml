# Serverless Devs configuration for Alibaba Cloud Function Compute (FC 3.0)
# Docs: https://manual.serverless-devs.com/user-guide/aliyun/#fc3
#
# Deploy:
#   s deploy
#
# Set credentials first:
#   s config add --AccessKeyID <id> --AccessKeySecret <secret> -a raaye

edition: 3.0.0
name: raaye-absa
access: default

vars:
  region: ap-southeast-1

resources:
  raaye-api:
    component: fc3
    props:
      region: ${vars.region}
      functionName: raaye-api
      description: "Raaye — AI-powered aspect-based sentiment analysis for Daraz reviews"
      runtime: custom.debian11
      handler: index.handler
      timeout: 90
      memorySize: 512
      diskSize: 512
      cpu: 0.35
      instanceConcurrency: 10
      customRuntimeConfig:
        command:
          - ./bootstrap
        port: 9000
      code: ./
      environmentVariables:
        DASHSCOPE_API_KEY: "sk-ws-H.DDMRYXL.o7nA.MEUCIQD4AGls6VeluDg78ghbtUkfEjIOPo_imq_vr6-8NXEuFwIgHbh9GRLV_m7cUG1sSAGjm_G2VBSHXqWgWWRTiGHk1mo"
        QWEN_MODEL: "qwen-plus-2025-07-28"
        DASHSCOPE_BASE_URL: "https://dashscope-intl.aliyuncs.com/compatible-mode/v1"
        DEMO_MODE: "false"
      triggers:
        - triggerName: http-trigger
          triggerType: http
          triggerConfig:
            authType: anonymous
            methods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
      asyncInvokeConfig:
        maxAsyncRetryAttempts: 0
